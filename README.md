# 🚀 AI Student Learner

Lightweight FastAPI service that uses Google Gemini + ChromaDB to answer questions, generate quizzes, and track learning history. Designed for quick demos and easy extension.

## ✨ Highlights
- ✅ FastAPI endpoints for Q&A and quizzes  
- 🤖 Answers generated by Gemini (Generative API)  
- 🧠 Quiz questions stored & evaluated server-side  
- 💾 Optional ChromaDB persistence for vectors/history  
- ⚡ Minimal setup — ready for local dev

## 🧩 Features
- Ask questions and get clear answers
- Generate quizzes (topic, difficulty, count)
- Submit quizzes and receive scores + explanations
- Simple in-memory history with optional vector storage

## 🛠 Quick Start (Windows PowerShell)
1. Clone repo and open backend folder:
   cd C:\Users\jefis\.anaconda\study_app_imp\backend
2. Create & activate venv:
   python -m venv .venv
   .\.venv\Scripts\Activate.ps1
3. Install deps:
   pip install -r requirements.txt
4. Set your API key (do NOT commit this):
   $Env:GOOGLE_API_KEY = "YOUR_KEY_HERE"
5. Run:
   uvicorn main:app --reload --host 0.0.0.0 --port 8000
6. Open interactive docs: http://localhost:8000/docs

## 🔐 Environment
- GOOGLE_API_KEY — Gemini / Generative API key (required if using Gemini)
- ChromaDB persistence directory can be configured in code (default: study_store)

## ⚠️ Common Gotchas
- 429 embedding/quota errors: enable billing or switch to a local embedding backend (sentence-transformers) to avoid Gemini free-tier limits.
- Do not commit API keys or .env files with secrets.

## 🧪 Endpoints (samples)
- POST /ask → AskQuestion -> { question }
- POST /quiz → Create quiz -> { topic, level, num_questions }
- POST /submit-quiz → Submit answers -> { quiz_id, user_answers }
- GET /history → View history
- POST /clear-history → Clear history
- GET /rewards → View reward points (demo)

## 📦 Contribution
PRs welcome — create issues for features or bugs. Keep changes small and documented.

## 📝 License
MIT — feel free to adapt.

Happy building! 🎓✨
